sudo: required

language: cpp

services:
  - docker

before_script:
  - docker build --tag devel -f Dockerfile.dev .

deploy:
  provider: releases
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  file: "BiobaseHTTPTools.tar.gz"
  skip_cleanup: true
  draft: true  
